rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary: Allow all reads and writes for data migration
    // TODO: Restore proper security after migration complete
    match /{document=**} {
      allow read, write: if true;
    }
  }
}